Prompt injection attempts include phrases like "ignore previous instructions", "system override", "reveal system prompt",
or instructions embedded in retrieved documents. Defenses include: separation of roles, data-only framing for retrieved text,
allow/block gating, output filtering, and logging + regression tests.
